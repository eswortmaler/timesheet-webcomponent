import { LitTimesheet } from '../src/lit-timesheet';
import { expect } from 'chai';

describe('First Test', () => { // the tests container
  it('start before end', () => {
    const timesheet = new LitTimesheet();
    //const entry = { date: "2000-01-01", start: "07:00", end: "08:00", category: "Meeting", description: "text", valid: true, dirty: false };
    //const uuid = "00000000-0000-0000-0000-000000000000";
    //timesheet.entries.set(uuid, entry);

    //expect(timesheet.validateEntry(entry, uuid)).to.be.true;
  });
});